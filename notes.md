npx expo install expo-dev-client
npm ls zustand | npm ls expo-dev-client | npm ls xxx-> check existing package

npx expo whoami
npx eas-cli@latest whoami -> more detailed
npx eas-cli@latest login -> login to expo

npm run tunnel -> connect from a tunnel to the app
files can be found : C:\users\orhun\.wslconfig -> NAT networking issue solved

npx eas-cli@latest build:configure --platform android